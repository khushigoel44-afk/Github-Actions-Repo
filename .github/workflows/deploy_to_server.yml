name: Deploy_to_Server

on: 
  workflow_call:

jobs: 
  deploy_Server:
    runs-on: self-hosted  # Targets your active green EC2 runner
    steps:
      - name: Native Deployment on EC2 Host
        run: |
          echo "Executing native bash directly inside the EC2 machine environment!"
          cd /home/ubuntu
          mkdir -p Devops
          echo "Directory verification and setup complete."