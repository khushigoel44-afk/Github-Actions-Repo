name: workflow Example

on: 
    push:
        branches: [main]

jobs:
    my_job:
        runs-on: ubuntu-latest
        steps:
            - name: Say Hello
              run: echo "Hello $USER"
        
